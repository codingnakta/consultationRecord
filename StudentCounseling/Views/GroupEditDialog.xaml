<Window x:Class="StudentCounseling.Views.GroupEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Height="560" Width="520"
        WindowStartupLocation="CenterOwner"
        Background="White" ResizeMode="CanResize" FontSize="13">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="집단명" Foreground="{StaticResource SubtleTextBrush}" FontSize="11" Margin="0,0,0,4"/>
        <TextBox Grid.Row="1" Style="{StaticResource InputBox}"
                 Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,14"/>

        <TextBlock Grid.Row="2" Text="참여 학생" Foreground="{StaticResource SubtleTextBrush}" FontSize="11" Margin="0,0,0,4"/>
        <Border Grid.Row="3" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6">
            <ListBox ItemsSource="{Binding StudentItems}" BorderThickness="0" Background="Transparent">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                  Content="{Binding Label}" Margin="6,4"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Style="{StaticResource GhostButton}" Content="취소" Margin="0,0,8,0" IsCancel="True"/>
            <Button x:Name="SaveBtn" Style="{StaticResource PrimaryButton}" Content="저장" IsDefault="True" Click="SaveBtn_Click"/>
        </StackPanel>
    </Grid>
</Window>
