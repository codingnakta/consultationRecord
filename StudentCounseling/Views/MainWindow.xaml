<Window x:Class="StudentCounseling.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:StudentCounseling.Views"
        Title="학생 상담 관리"
        Height="780" Width="1320"
        WindowStartupLocation="CenterScreen"
        Background="White"
        FontSize="13">
    <Grid>
        <TabControl Margin="12" BorderThickness="0" Background="Transparent">
            <TabControl.Resources>
                <Style TargetType="TabItem">
                    <Setter Property="Padding" Value="14,8"/>
                    <Setter Property="FontWeight" Value="Medium"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TabItem">
                                <Border x:Name="b" BorderThickness="0,0,0,2" BorderBrush="Transparent" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter x:Name="HeaderPresenter" ContentSource="Header"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="HeaderPresenter" Property="TextElement.Foreground" Value="{StaticResource AccentBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TabControl.Resources>

            <TabItem Header="상담">
                <views:CounselingTab DataContext="{Binding CounselingTab}"/>
            </TabItem>
            <TabItem Header="통계">
                <views:StatsTab DataContext="{Binding StatsTab}"/>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
