<Window x:Class="StudentCounseling.Views.StudentEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Height="440" Width="420"
        WindowStartupLocation="CenterOwner"
        Background="White" ResizeMode="NoResize" FontSize="13">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="이름" Foreground="{StaticResource SubtleTextBrush}" FontSize="11" Margin="0,0,0,4"/>
        <TextBox Grid.Row="1" Style="{StaticResource InputBox}"
                 Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12"/>

        <TextBlock Grid.Row="2" Text="학년 / 반 / 번호" Foreground="{StaticResource SubtleTextBrush}" FontSize="11" Margin="0,0,0,4"/>
        <Grid Grid.Row="3" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Style="{StaticResource InputBox}" Text="{Binding Grade, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox Grid.Column="2" Style="{StaticResource InputBox}" Text="{Binding ClassNumber, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox Grid.Column="4" Style="{StaticResource InputBox}" Text="{Binding Number, UpdateSourceTrigger=PropertyChanged}"/>
        </Grid>

        <TextBlock Grid.Row="4" Text="메모" Foreground="{StaticResource SubtleTextBrush}" FontSize="11" Margin="0,0,0,4"/>
        <TextBox Grid.Row="5" Style="{StaticResource InputBox}"
                 Text="{Binding Memo, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" MinHeight="80"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Style="{StaticResource GhostButton}" Content="취소" Margin="0,0,8,0" IsCancel="True"/>
            <Button x:Name="SaveBtn" Style="{StaticResource PrimaryButton}" Content="저장" IsDefault="True" Click="SaveBtn_Click"/>
        </StackPanel>
    </Grid>
</Window>
